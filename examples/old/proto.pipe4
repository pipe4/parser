import (
    "github.com/pegilang/example/pipeline/protobuilder/proto_build"
    "github.com/pegilang/core"
)

proto_build({from: core.source.resolve("./proto"), to: core.source.resolve("./cpp"), lang: "cpp"})

core.Run(this)
